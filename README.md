# Workshop Landing — OpenClaw · AI Agents VN

Landing page (một file HTML self-contained) cho workshop "X3 năng suất bán hàng nhờ AI".

- **Preview:** xem GitHub Pages của repo này.
- `index.html` — trang đã publish.
- `assets/openclaw-logo.svg` — logo gốc dạng SVG (trang đã nhúng sẵn bản inline).

## Cập nhật trang

File nguồn khi chỉnh sửa là `Workshop Landing_redesign.html` (không commit). Sau khi sửa:

```bash
cp "Workshop Landing_redesign.html" index.html
git add index.html && git commit -m "Update landing page" && git push
```
